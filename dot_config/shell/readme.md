# Personal zsh shell config

To properly start, run this in the user folder "~" (adjust actual path to zshrc):

```bash
ln -s .config/zsh/zshrc .zshrc
```

This creates a symbolic link to the actual config file.
